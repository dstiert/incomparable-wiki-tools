\{{Show_info_box |
    image1 = {{ Image }} |
    title = {{ Title }} |
    subtitle = {{ SubTitle }} |
    show = [[{{ Show }}]] | 
    episode_number = {{ EpisodeNumber }} |
    duration = {{ Duration }} |
    explicit = {{ Explicit }} |
    host = {{ Host }} |
    panelists = {{#each Panelists }}[[{{ this }}]] {{/each}} | 
    published = {{ Published }} |
    link = {{ Link }} |
    previous = [[{{ Previous }}]] |
    next = [[{{ Next }}]]
}}

{{ Description }}